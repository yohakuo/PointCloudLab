# 阶段 3 较大步长联合搜索复核

两次新运行都使用代码提交 `a2d185d`、相同的数据与配置，输入和配置在各自运行期间未变化。

| 运行 | OMP / OpenBLAS / MKL 线程 | 规范候选数 | 与历史较大步长运行的候选 JSON |
| --- | --- | ---: | --- |
| `20260925-grid-joint-fast-recorded` | 均未显式设置 | 5 | 不同 |
| `20260925-grid-joint-fast-fixed-threads` | 均为 1 | 4 | 逐字节一致 |

历史运行有 4 个规范候选。两个新运行的原始候选均为 8 个。线程设置与结果差异同时出现，说明复现时必须保存并遵守线程配置；现有两次观测不足以量化所有环境因素的影响。完整参数、输入 SHA-256、报告和归档哈希见各自的 `manifest.json`。
